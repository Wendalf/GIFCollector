<div class="row">
	<div class="col-sm-3"></div>
	<div class="col-sm-6">
		<img src="../users/<%=  @gif.user_id %>/<%= @gif.filename %>">
		<p><%= @gif.description %></p>
		<% @gif.tags.each do |tag| %>
		<a href="/tags/<%= tag.slug %>">#<%= tag.name %></a>
		<% end %>
	</div>
	<div class="col-sm-3"></div>
</div>