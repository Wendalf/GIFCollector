<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>GIFCollector</title>
  </head>
  <body>
  	<nav>
  	<% if logged_in? %>

       <a href="/"> Home  </a> |
       <a href="/users/<%= @user.id %>">My Gifs</a> |
       <a href="/signout"> Log Out </a>
       <p>Welcome, <%=current_user.username %></p>

     <% else %>
       <a href="/login"> Log In </a>
       <a href="/signup">Sign Up</a>

     <%end %>

	</nav>
     <%= yield %>
  </body>
</html>
